sudo: false
language: python
python:
  - "2.7"
install:
    - pip install -U coverage coveralls
    - python bootstrap.py
    - bin/buildout
script:
    - coverage run bin/test -pvc
after_success:
    - coveralls
notifications:
    email: tseaver@palladion.com
